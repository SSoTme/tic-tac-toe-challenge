
var intermediateBrain = {};
intermediateBrain.play = function (board, availableCells) {
    // Play randomly until a human writes custom code to control this Brain
    return randomBrain.play(board, availableCells);
}
                  